[package]
name = "tx-builder-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde = { version = "1", features = ["derive"] }
once_cell = "1.17"
serde_json = "1.0"

transfer = { path = "../transfer/gen" }

